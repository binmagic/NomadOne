# planner/
> L2 | 父级: /CLAUDE.md

成员清单
planner-workspace.tsx: 详情页规划工作台。自动规划 POST 显式携带 previewConfig；输出配置卡片只读。生成设置含第一张主图锁定参考图标题字体。双语视觉 Prompt 旁「重新生成 Prompt」按当前标题/文案重写该字段。新增模块默认 visualPrompt 禁止 ACT 购买按钮。

法则: 成员完整·一行一文件·父级链接·技术词前置
[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
