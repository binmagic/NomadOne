# storage/
> L2 | 父级: /CLAUDE.md

成员清单
asset-manager.ts: 商品素材写入 uploads/generated/exports 并建 ProductAsset；对话生图写入 studio/{userId}/{conversationId}，只回相对路径；删会话/删用户时递归清磁盘。

法则: 成员完整·一行一文件·父级链接·技术词前置
[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
