# forbidden-words/
> L2 | 父级: /CLAUDE.md

成员清单
route.ts: GET 工作区违禁词列表，POST 新建（word 或 words[]，重复跳过）。登录用户共用一张表。
[id]/route.ts: PATCH 改词，DELETE 删词。大小写不敏感判重。

法则: 成员完整·一行一文件·父级链接·技术词前置
[PROTOCOL]: 变更时更新此头部，然后检查 CLAUDE.md
